# dmz-lab

Implementar una DMZ segura en Cisco Packet Tracer que permita acceso web público (HTTP/HTTPS) al servidor DMZ desde Internet sin exponer la LAN interna, usando NAT estático + ACL extendidas con política "deny by default".
